# Changelog

## 1.2.9

- یک سیستم چیدمان و فاصله‌گذاری مشترک برای همه صفحات مدیریت، اپراتور، وکیل، مشتری و صفحات عمومی اضافه شد.
- فاصله عمودی داشبوردها، گریدها، کارت‌ها، اعلان‌ها، جدول‌ها و وضعیت‌های خالی در دسکتاپ و موبایل یک‌دست شد.
- کارت‌های داخل ردیف‌ها و گریدهای چندستونه دیگر margin قالب را با gap شبکه جمع نمی‌کنند و بلوک‌ها به هم نمی‌چسبند.
- padding کارت‌ها و فاصله کنترل‌های هدر در breakpointهای دسکتاپ، تبلت و موبایل بازتنظیم شد.
- تست ایستای جلوگیری از بازگشت مشکلات layout و گزارش ممیزی رابط کاربری ۵۴ قالب اضافه شد.

## 1.2.8

- سیستم فرم مشترک برای هسته و پلاگین‌ها اضافه شد و ورودی‌ها، selectها، input groupها و دکمه‌های مجاور روی baseline واحد قرار گرفتند.
- سربرگ چاپ قرارداد با شماره قرارداد در راست، لوگوی مجموعه در مرکز، تاریخ و نام مشتری در چپ و عنوان مستقل بازطراحی شد.
- مسیر کامل هاست از جدول عادی پلاگین حذف و فقط در بخش بسته اطلاعات فنی مدیر نمایش داده می‌شود.
- چرخه کشف، بازاسکن، بارگذاری مجدد و نصب پلاگین حذف‌شده با وضعیت‌های canonical و registry reconciliation اصلاح شد.
- اسکرول سایدبار با viewport پویا و فضای امن پایین اصلاح شد تا آخرین گزینه در دسترس بماند.
- ابزار انتشار مستقل برای ZIP نصب کامل، بسته بروزرسانی دارای manifest و ZIP پلاگین حسابداری اضافه شد.

## 1.2.7

- ارتفاع و اسکرول سایدبار اصلاح شد تا آخرین گزینه در همه‌ی ارتفاع‌های صفحه قابل مشاهده باشد.
- baseline مشترک فرم‌ها، تراز ورودی‌ها و جایگاه علامت الزامی labelها یکدست شد.
- حذف دائمی قرارداد به آیکن سریع کنار ویرایش در بالای کارت منتقل شد.

## 1.2.6

- نسخه‌گذاری semantic اصلاح شد؛ مقدار خطی قبلی `1.0.26` اکنون به `V1.2.6` تبدیل شده است.
- نصب افزونه حسابداری با اصلاح migration حساب کاربران و نمایش خطای واقعی پایدار شد.
- آیکن مدال‌ها به آیکن Award معتبر قالب متصل شد و collapse سایدبار با وضعیت واقعی قالب همگام شد.

## 1.0.25

- اصلاح ناوبری کارت قرارداد با listener تفویض‌شده و تفکیک فرم bulk؛ کلیک روی فضای کارت، نام، شماره و آمار اکنون به جزئیات قرارداد می‌رود و کنترل‌های داخلی مستقل می‌مانند.
- اصلاح لغو قرارداد با محدودکردن اصلاحیه به پرداخت‌های همان قرارداد، پشتیبانی از پیش‌پرداخت، ثبت snapshot و Audit و جلوگیری از تغییر پرداخت قراردادهای دیگر مشتری.
- وضعیت تسویه canonical شد: قرارداد دارای تعهدات کامل پرداخت‌شده به `completed` با عنوان «تسویه‌شده» می‌رود و قرارداد لغوشده هرگز دوباره فعال نمی‌شود.
- اصلاح مسیر اصلاحیه برای بازگشت ایمن `completed` به `active`، جلوگیری از پرداخت یا قسط جدید برای قرارداد تسویه‌شده و پشتیبانی از migration پرداخت‌های پیش‌پرداخت.
- نمایش آواتارها با catalog مرکزی و `<img>` واقعی، fallback کنترل‌شده و URL نسخه‌گذاری‌شده اصلاح شد.
- نسخه service worker و cache دارایی به‌روزرسانی شد و گزارش دیباگ قرارداد و آواتار اضافه شد.

## 1.0.24

- کلیک روی کارت قرارداد مستقیماً جزئیات همان قرارداد را باز می‌کند و دکمه جزئیات تکراری حذف شد؛ ویرایش کارت با آیکن مداد در گوشه بالا-چپ انجام می‌شود.
- قرارداد پس از پرداخت کامل اقساط به‌صورت خودکار بسته و غیرفعال می‌شود و هنگام ویرایش برنامه اقساط و متن چاپی قرارداد دوباره تولید می‌شوند.
- لغو قرارداد امکان ثبت اصلاحیه اختیاری برای صفر کردن پرداخت‌های موفق مشتری را با حفظ سوابق مالی فراهم می‌کند.
- مرتب‌سازی سررسیدهای گذشته بر اساس تاریخ، مبلغ و نام مشتری و کنترل‌های هم‌راستا و چک‌باکس‌های مدرن اضافه شد.
- آواتار پیش‌فرض کاربران به‌صورت پایدار و متنوع انتخاب می‌شود تا کاربران بدون آواتار دستی همیشه تصویر یکسان نبینند.

## 1.0.23

- اصلاح ویرایش قرارداد: تغییرات مالی و زمان‌بندی، اقساط پرداخت‌نشده را همگام می‌کند و اقساط پرداخت‌شده و سفارشی حفظ می‌شوند.
- حذف الزام بی‌دلیل برای وارد کردن علت ویرایش و نمایش آواتارهای تصویری با مسیر نسخه‌گذاری‌شده در همه‌ی پنل‌ها.
- یکسان‌سازی خط مبنای ورودی‌ها، دکمه‌ها و شبکه‌ی تکرارشونده‌ی فرم‌ها برای تراز بهتر در دسکتاپ و موبایل.
- اصلاح اعتبارسنجی ZIP بروزرسانی برای بسته‌هایی که ورودی‌های ریشه‌ی `./` دارند و جلوگیری از دوباره‌چسباندن timestamp به نام استاندارد بسته.

## 1.0.22

- استانداردسازی نمایش نسخه با قالب `vMAJOR.MINOR.PATCH` و نمایش نسخه نصب‌شده در تنظیمات.
- افزودن کلیدهای فعال‌سازی مستقل برای تجارت الکترونیک و صفحه لندینگ و مسدودسازی routeهای غیرفعال.
- بهینه‌سازی داشبوردهای مدیریت، اپراتور، واحد حقوقی و مشتری با KPI و اقدام‌های سریع‌تر.
- بازسازی چیدمان تنظیمات و افزودن وضعیت قابلیت‌های فروشگاه.
- افزودن شش آواتار تصویری پیش‌فرض با رنگ‌بندی متنوع برای کاربران.

## 1.0.21

- افزودن چاپ مستقیم قرارداد از کارت و لیست قراردادها با پشتیبانی از قراردادهای فعال، تکمیل‌شده و لغوشده.
- افزودن لغو امن قرارداد بدون حذف فیزیکی، لغو اقساط فعال، ثبت علت، تاریخچه تغییر، لاگ ممیزی و اعلان مشتری.
- جلوگیری از لغو قرارداد دارای پیش‌پرداخت، پرداخت تأییدشده یا قسط پرداخت‌شده و حذف قرارداد از معوقات، جریمه، مطالبات و شاخص‌های ریسک.
- جایگزینی فهرست کامل مشتریان در فرم قرارداد با جست‌وجوی Ajax محدود، انتخاب با صفحه‌کلید و بررسی تعارض کد ملی، موبایل و ایمیل.
- اصلاح کنترل‌های گزارش‌های هوشمند، حقوقی و امتیازها برای نادیده‌گرفتن قراردادهای لغوشده.

## 1.0.20

- اصلاح redirectهای سامانه روی PHP ویندوز و جلوگیری از خطای `http://index.php` پس از ورود.
- جلوگیری از خطاهای JavaScript ساعت و منوی کناری در صفحه‌هایی که این اجزا را ندارند.
- اصلاح import بسته‌های انتقالی با حفظ کامنت‌های اجرایی MySQL و کلیدهای خارجی dump.
- بسته کامل این نسخه همراه با dump اسکیمای تمیز و قابل نصب ارائه می‌شود.

## 1.0.0

- آماده‌سازی نسخه قابل نصب Proma Pay برای XAMPP و مارکت راست‌چین.
- محدودسازی دسترسی اپراتور به سررسید گذشته و گفت‌وگو.
- افزودن modal های تماس، ارجاع حقوقی و ثبت نتیجه پیگیری در سررسید گذشته.
- افزودن فیلترها و pagination بخش اقساط.
- اعمال فونت YekanBakh در پنل، فرم‌ها، modal ها، صفحه ورود، نصب و چاپ.
- تکمیل اطلاعات توسعه‌دهنده، requirements و فایل نمونه دیتابیس.
- اصلاح نصب نیمه‌کاره، حذف حرف اضافه `پ` از صفحه نصب، و ثبت لاگ خطای نصب.
- افزودن فراموشی رمز عبور با کد پیامکی و اتصال به API پنل پیامکی IPPanel.
