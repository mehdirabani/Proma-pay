# Changelog

## 1.0.22

- استانداردسازی نمایش نسخه با قالب `vMAJOR.MINOR.PATCH` و نمایش نسخه نصب‌شده در تنظیمات.
- افزودن کلیدهای فعال‌سازی مستقل برای تجارت الکترونیک و صفحه لندینگ و مسدودسازی routeهای غیرفعال.
- بهینه‌سازی داشبوردهای مدیریت، اپراتور، واحد حقوقی و مشتری با KPI و اقدام‌های سریع‌تر.
- بازسازی چیدمان تنظیمات و افزودن وضعیت قابلیت‌های فروشگاه.
- افزودن شش آواتار تصویری پیش‌فرض با رنگ‌بندی متنوع برای کاربران.

## 1.0.21

- افزودن چاپ مستقیم قرارداد از کارت و لیست قراردادها با پشتیبانی از قراردادهای فعال، تکمیل‌شده و لغوشده.
- افزودن لغو امن قرارداد بدون حذف فیزیکی، لغو اقساط فعال، ثبت علت، تاریخچه تغییر، لاگ ممیزی و اعلان مشتری.
- جلوگیری از لغو قرارداد دارای پیش‌پرداخت، پرداخت تأییدشده یا قسط پرداخت‌شده و حذف قرارداد از معوقات، جریمه، مطالبات و شاخص‌های ریسک.
- جایگزینی فهرست کامل مشتریان در فرم قرارداد با جست‌وجوی Ajax محدود، انتخاب با صفحه‌کلید و بررسی تعارض کد ملی، موبایل و ایمیل.
- اصلاح کنترل‌های گزارش‌های هوشمند، حقوقی و امتیازها برای نادیده‌گرفتن قراردادهای لغوشده.

## 1.0.20

- اصلاح redirectهای سامانه روی PHP ویندوز و جلوگیری از خطای `http://index.php` پس از ورود.
- جلوگیری از خطاهای JavaScript ساعت و منوی کناری در صفحه‌هایی که این اجزا را ندارند.
- اصلاح import بسته‌های انتقالی با حفظ کامنت‌های اجرایی MySQL و کلیدهای خارجی dump.
- بسته کامل این نسخه همراه با dump اسکیمای تمیز و قابل نصب ارائه می‌شود.

## 1.0.0

- آماده‌سازی نسخه قابل نصب Proma Pay برای XAMPP و مارکت راست‌چین.
- محدودسازی دسترسی اپراتور به سررسید گذشته و گفت‌وگو.
- افزودن modal های تماس، ارجاع حقوقی و ثبت نتیجه پیگیری در سررسید گذشته.
- افزودن فیلترها و pagination بخش اقساط.
- اعمال فونت YekanBakh در پنل، فرم‌ها، modal ها، صفحه ورود، نصب و چاپ.
- تکمیل اطلاعات توسعه‌دهنده، requirements و فایل نمونه دیتابیس.
- اصلاح نصب نیمه‌کاره، حذف حرف اضافه `پ` از صفحه نصب، و ثبت لاگ خطای نصب.
- افزودن فراموشی رمز عبور با کد پیامکی و اتصال به API پنل پیامکی IPPanel.
