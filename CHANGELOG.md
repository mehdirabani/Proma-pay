# Changelog

## V1.5.6

- مبنای آغاز جریمه حقوقی به زمان ارجاع رسمیِ ثبت‌شده و قابل ممیزی محدود شد؛ تشکیل پرونده داخلی یا پیش‌نویس به‌تنهایی بدهی حقوقی ایجاد نمی‌کند.
- برآورد جریمه حقوقیِ مقایسه‌ای از بدهی، تسویه، درگاه، رسید و حسابداری جدا شد و فقط با نشان «فعلاً اعمال نشده» به مشتری نمایش داده می‌شود.
- گزارش‌های مالی مدیریت و حقوقی تفکیک جریمه عادی واقعی، حقوقی واقعی، برآورد تحلیلی و جمع قابل پرداخت واقعی را نمایش می‌دهند.

## 1.5.5

- همهٔ فیلترهای اصلی پرداخت‌ها، مشتریان، کاربران و قراردادها بر مبنای ارتفاع کنترل ۴۸ پیکسل، فاصلهٔ ثابت و چینش واکنش‌گرا یکپارچه شدند؛ بنابراین input، select و دکمه‌ها در RTL و موبایل هم‌راستا می‌مانند.
- عملیات تماس در «مدیریت اقساط» و «سررسیدهای گذشته» به مودال عملیاتی تبدیل شد: شماره‌های مشتری و ضامن‌ها با واکشی گروهی نمایش داده می‌شوند و هر شماره تماس مستقیم و کپی مستقل دارد.
- دفترچهٔ اقساط دیگر به CSS یا JavaScript درون‌خطی وابسته نیست؛ در تب مستقل و هنگام چاپ A4 با استایل خارجی، کارت مشخصات و برگه‌های خوانا رندر می‌شود.
- کارت‌های مدال از ارتفاع کشسان خارج شدند و رنگ/انتخاب مدال دیگر خطای CSP ایجاد نمی‌کند.
- ترتیب سایدبار برای نقش‌های مدیر، اپراتور، وکیل و مشتری بر اساس جریان کار بازچینش شد؛ موارد عملیاتیِ پرتکرار در ابتدای هر نقش قرار دارند.

## 1.5.4

- بستهٔ نصب از دارایی‌های نمونه و بدون‌استفادهٔ `html/RTL` پاک‌سازی شد؛ این فایل‌ها در اجرای Proma Pay مصرف نمی‌شدند و علت اصلی حجم غیرعادی بستهٔ ۱.۵.۳ بودند.
- فیلتر «قراردادهای دارای پروندهٔ حقوقی فعال نمایش داده نشوند» به مدیریت اقساط و صف پیگیری سررسیدها افزوده شد؛ فیلتر در سرور اعمال می‌شود و شمارش، صفحه‌بندی و تب‌ها با آن هماهنگ‌اند.
- چیدمان فیلترهای عملیاتی برای checkbox، input، select و دکمه‌ها در دسکتاپ و موبایل هم‌تراز شد.
- نسخهٔ cache دارایی‌های مرورگر به‌روزرسانی شد تا رابط ۱.۵.۴ بلافاصله پس از نصب دریافت شود.

## 1.5.3

- بازطراحی عملیاتی صفحات اقساط و سررسیدهای گذشته با تب، فیلتر فشرده، Empty State و صفحه‌بندی فشرده.
- تجمیع صف سررسید در سطح قرارداد و فیلتر/مرتب‌سازی امن سمت سرور.
- بازطراحی سوابق خرید مشتری با یک کارت یکپارچه برای هر قرارداد.
- بازنشانی امن شماره صفحه پس از تغییر فیلترهای AJAX.

## 1.5.2

- زبان بصری مشترک پنل‌ها بازطراحی شد: کارت‌ها، عنوان‌ها، جدول‌ها، فاصله‌ها، حالت تمرکز و نمایش موبایل اکنون با ریتم فشرده، خوانا و راست‌به‌چپ یکسان رندر می‌شوند.
- صفحه «سوابق خرید» مشتری از نو چیدمان شد؛ سه شاخص تمام‌عرض، سوابق قرارداد، وضعیت، زمان‌بندی پرداخت و برنامه اقساط هرکدام ارتفاع مستقل دارند و کارت کوتاه دیگر به ارتفاع کارت بلند کش نمی‌آید.
- سوابق مشتری در موبایل تک‌ستونه و جدول‌ها در محفظهٔ اسکرول افقی کنترل‌شده باقی می‌مانند تا داده‌ها بریده یا از عرض صفحه خارج نشوند.
- سلول‌های عملیات جدول‌ها به‌صورت سلول واقعی جدول حفظ می‌شوند؛ این کار تراز دکمه‌های مشاهده/اصلاح/حذف را در همه پنل‌ها پایدار می‌کند.

## 1.5.1

- migration موتور تسویهٔ V1.5.0 اصلاح شد: تعریف جدول `settlement_quotes` فقط یک کلید اصلی دارد. به‌روزرسانی V1.5.1 migration ناموفق V1.5.0 را دوباره و ایمن اجرا می‌کند تا خطای `Multiple primary key defined` و خطای ۵۰۰ پرداخت برطرف شود.
- دکمه‌های «مشاهده» و «اصلاحیه» در جدول گزارش پرداخت داخل یک wrapper انعطاف‌پذیر قرار گرفتند؛ سلول جدول دیگر flex نمی‌شود و ردیف‌ها در RTL/Chromium هم‌راستا می‌مانند.

## 1.5.0

- موتور تسویه اقساط به محاسبه مرکزیِ صحیح با مبالغ صحیح (integer)، تخصیص قطعی پرداخت جزئی، quote کوتاه‌عمر، idempotency و قفل تراکنشی مجهز شد. داده‌های قدیمی که ظاهراً وضعیت «تسویه» داشتند اما مانده واقعی داشتند، دیگر مانع تسویه مبلغ باقی‌مانده نمی‌شوند.
- ماژول حقوقی به سیاست نسخه‌دار برای هر قرارداد، صف محدودِ قراردادهای واجد شرایط، تشکیل داخلی پرونده توسط واحد حقوقی با جلوگیری از تکرار و snapshot بدهی مجهز شد.
- اخطار قراردادی، پیش‌نویس دادخواست/شکواییه و پرونده داخلی به‌صراحت از ابلاغ یا ثبت رسمی قضایی تفکیک شدند. تایید ثبت بیرونی فقط با تایید صریح کاربر حقوقی، مرجع، تاریخ و شماره پیگیری واقعی ممکن است.
- حذف پرونده حقوقی به بایگانی غیرمخرب تغییر کرد تا لاگ‌ها، پرداخت‌ها و اسناد برای پیگیری محفوظ بمانند.

## 1.4.9

- ریشهٔ burst مسیر `contracts/preview` برطرف شد: فرم‌های ویرایش مخفی دیگر هنگام بارگذاری صفحه هیچ درخواست محاسبه‌ای نمی‌فرستند. هر فرم فقط پس از تغییر واقعی کاربر، با debounce، لغو درخواست قبلی، کنترل پاسخ کهنه، کنترل دیده‌بودن مودال و cache کوتاه‌مدت پیش‌نمایش می‌گیرد.
- endpoint پیش‌نمایش قرارداد اکنون ورودی محدود و معتبر می‌پذیرد، پاسخ بدون cache می‌دهد، session را آزاد می‌کند، pluginهای نامرتبط را boot نمی‌کند و سقف اختصاصی بسیار کم برای هاست اشتراکی دارد.
- سازندهٔ update از این نسخه ZIP تفاضلی واقعی می‌سازد: فایل‌های target با manifest آرشیو نصبِ پایدارِ قبلی مقایسه می‌شوند و فقط فایل یا migration جدید/تغییریافته وارد بسته می‌شود. نام استاندارد هر نسخه نیز فقط یک بار تولید می‌شود.

## 1.4.8

- محافظ درخواستِ بدون وابستگی به دیتابیس پیش از session، افزونه و queryهای برنامه اجرا می‌شود. برای هر IP سقف token-bucket دارد و مسیرهای poll گفت‌وگو، اعلان و health را جداگانه محدود می‌کند تا حلقه مرورگر یا افزونه مخرب باعث مسدود شدن IP روی هاست اشتراکی نشود.
- فاصله حداقل میان fetchهای برنامه در هر تب اعمال شد؛ پاسخ 429 با `Retry-After` باعث توقف موقت درخواست‌های بعدی همان تب می‌شود.
- poll گفت‌وگو از ۱۵ ثانیه به ۶۰ ثانیه و اعلان از ۴۵ ثانیه به ۱۸۰ ثانیه تغییر کرد؛ در تب پنهان نیز بازه‌ها به چند دقیقه افزایش می‌یابد. ارسال پیام همچنان poll را بیدار می‌کند.
- کش یک‌ساله و immutable برای دارایی‌های versioned رابط RTL افزوده شد تا با هر جابه‌جایی صفحه، ده‌ها فایل CSS، JS، فونت و تصویر دوباره از هاست اشتراکی درخواست نشوند.
- نام‌های alias تکراری با الگوی `1-4-8` از سازنده انتشار حذف شد. از این نسخه به بعد برای هر بسته فقط نام استاندارد `PromaPay-vX.Y.Z.zip` یا `PromaPay-Update-vX.Y.Z.zip` تولید می‌شود.

## 1.4.7

- مسیر فهرست قراردادها برای هاست اشتراکی بازطراحی شد: آمار اقساط، نمودار پرداخت، تایم‌لاین، ضامنان و داده‌های فرم ویرایشِ قراردادهای یک صفحه با واکشی گروهی دریافت می‌شوند و الگوی پرهزینهٔ query برای هر کارت حذف شد.
- بازدید عادی از قراردادها دیگر همگام‌سازی سراسریِ نوشتنی وضعیت قراردادها را اجرا نمی‌کند؛ پس از پرداخت فقط همان قرارداد همگام می‌شود.
- پیش‌نمایش لغو و حذف دائمی قرارداد فقط هنگام باز شدن همان پنجره دریافت می‌شود و کنترل نهایی همچنان در سرور انجام می‌گیرد.
- ایندکس‌های مسیر کارت قرارداد و پرداخت افزوده شد و پاک‌سازی اولیهٔ تنظیمات از درخواست‌های معمول به migration منتقل شد تا درخواست‌های خواندنی روی جدول settings قفل نگیرند.

## 1.4.6

- فایل‌های runtime قالب RTL شامل CSS، JavaScript، فونت‌ها و تصاویر لازم در بسته نصب و بسته بروزرسانی قرار گرفتند تا مسیرهای `html/RTL/assets` روی نصب‌های هاست با پاسخ 404 مواجه نشوند.
- اعتبارسنجی انتشار اکنون وجود دارایی‌های حیاتی رابط ورود و داشبورد را در آرشیو کنترل می‌کند.
- مسیر خروجی بسته بروزرسانی در تمام مراحل ساخت و بررسی به `dist/updates` منتقل شد؛ بسته نصب کامل همچنان در `dist/core` ساخته می‌شود.

## 1.4.3

- حذف دائمی قرارداد برای مدیر با پاکسازی اختیاری سوابق مالی، رسیدها، اسناد و وابستگی‌های قابل حذف، snapshot کامل، ثبت ممیزی و هشدار مستقل برای پرداخت درگاهی اضافه شد.
- قفل ورود مبتنی بر IP مشترک حذف و محدودسازی به شناسه کاربر و ترکیب شناسه/IP محدود شد تا کاربران پشت NAT یا IP مشترک یکدیگر را مسدود نکنند.
- تأیید اصلاح مشخصات به صفحه مستقل مدیریتی با جست‌وجو، فیلتر، صفحه‌بندی، تأیید جزئی، رد، تعارض و تاریخچه کامل هر حساب منتقل شد.
- حلقه پیشرفت تسویه قرارداد بازگردانده و کارت‌های مدال، badgeها و عملیات آن‌ها برای متن بلند و نمایش موبایل اصلاح شدند.
- صفحه سلامت سامانه با ظاهر بومی، وضعیت سرویس‌ها، افزونه‌ها و تله‌متری ایمن بازطراحی شد و مسیر و جزئیات حساس سرور نمایش داده نمی‌شود.
- تنظیمات اتصال دیتابیس از متغیر محیطی پشتیبانی می‌کند و migration ترمیمی V1.4.3 قفل‌های قدیمی IP و ایندکس صف تأیید مشخصات را اصلاح می‌کند.
- چیدمان مشترک مودال‌ها به ردیف‌های خودکار، بدنه اسکرول‌پذیر محدود و لیبل صریح مجهز شد تا textarea، متن راهنما و فیلد بعدی در هیچ اندازه‌ای روی هم قرار نگیرند.
- پیش‌نمایش پرداخت مودال‌های بسته lazy شد؛ جستجوها و پیش‌نمایش‌های تکراری از کش محدود و لغو درخواست قبلی استفاده می‌کنند و فرم قسط دلخواه در برابر ارسال دوباره محافظت می‌شود.

## 1.4.1

- کنترل مرکزی تسویه قسط، محاسبه مبلغ قابل پرداخت روز و جلوگیری از پرداخت تکراری در پرداخت تکی و گروهی اضافه شد.
- تعمیر قراردادهای تکراری با حفاظت از سوابق مالی، لغو تراکنشی و ابزار تطبیق مالی ایمن شد.
- چرخه نصب، فعال‌سازی، غیرفعال‌سازی و بازیابی پلاگین‌ها یکپارچه و مشکل queryهای بدون بافر در migrationها رفع شد.
- اعلان‌ها، گفت‌وگو، تقویم، مدال‌ها و تأیید مرحله‌ای تغییر مشخصات برای هر کاربر و نقش پایدار شدند.
- نصب تازه و ارتقا از V1.4.0 به اسکیما یکسان می‌رسند و DDL زمان درخواست با کنترل صریح اسکیما جایگزین شد.
- چاپ قرارداد A4 با سربرگ فشرده و خوانا، بدون scale یا zoom، در مرورگر واقعی یک‌صفحه‌ای تأیید شد.
- گیت انتشار سخت‌گیرانه، manifest تفاضلی بروزرسانی، بررسی ZIP و بسته مستقل Proma Accounting V1.2.4 اضافه شد.

## 1.4.0

- خطای HTTP 500 پرداخت دستی از جزئیات قرارداد با حذف ناسازگاری متد `PaymentRequest` و مدل پایه رفع شد.
- ایجاد قرارداد به کلید یکتای تراکنشی و قفل ارسال مرورگر مجهز شد تا کلیک یا درخواست تکراری فقط یک قرارداد بسازد.
- تغییر آواتار برای همه نقش‌ها فوری شد و اصلاح سایر مشخصات با مقایسه مقدار فعلی و پیشنهادی در صف تأیید مدیریت قرار گرفت.
- رمز و آواتار از payload تأیید مشخصات حذف و ورود مشتری بدون تغییر قانون کد ملی و چهار رقم آخر موبایل، به محدودسازی تلاش مجهز شد.
- انتخاب‌گر قراردادها و رابط وابسته حذف و schema نصب تازه و migrationهای چاپ قرارداد برای تولید سند از اولین قرارداد ترمیم شدند.

## 1.3.9

- طراحی واحد فیلدهای filled و بدون کادر برای هسته، پنل مشتری و افزونه‌ها اضافه شد؛ focus، حالت خطا/غیرفعال، کنترل‌های انتخابی و موبایل همگی قرارداد بصری مشترک دارند.
- کارت قرارداد به ساختار هویت، کنترل و اطلاعات مالی تفکیک شد؛ انتخاب دسته‌جمعی دیگر روی محتوا نمی‌افتد و تعامل با checkbox یا actionها مسیر کارت را تغییر نمی‌دهد.
- قسط دلخواه اکنون توضیح قابل مشاهده مشتری، یادداشت داخلی و وضعیت نمایش مستقل دارد؛ نمایش یادداشت داخلی به نقش مجاز محدود شد.
- migration ترمیمی برای ستون‌های document چاپ قرارداد و قسط دلخواه اضافه شد تا نصب‌های قدیمی هنگام تولید قرارداد با خطای ستون ناشناخته روبه‌رو نشوند.
- چاپ قرارداد با سربرگ ساخت‌یافته و preset فشرده بدون `zoom` یا `transform` تثبیت شد؛ footer قابل ویرایش مدیر و مقدار پیش‌فرض جدید دارد.
- خطای کنترل‌شده در CLI با کد خروجی غیرصفر خاتمه می‌یابد تا تست‌های انتشار شکست واقعی را پنهان نکنند.

## 1.3.8

- آیکن‌های عملیات کارت‌های مشتری و قرارداد به SVG داخلی مقاوم در برابر شکست icon font تبدیل و چینش نام‌های بلند و عملیات کارت در تبلت و موبایل تثبیت شد.
- لغو قرارداد در آزمون کامل UI و دیتابیس بازبینی شد؛ قرارداد و همه اقساط فعال آن لغو می‌شوند، سابقه مالی محفوظ می‌ماند و قرارداد از معوقات کنار می‌رود.
- تماس سررسید گذشته برای مدیر فعال و دکمه کپی شماره در بارگذاری اولیه صفحه bind شد؛ مرتب‌سازی و نمایش جریمه‌های عادی/قابل اعمال نیز حفظ شدند.
- تقویم مدیریت از رخدادهای خودکار سررسید قسط جدا شد و فقط مشتری رخدادهای سیستمی اقساط خود را می‌بیند.
- مدیریت مدال‌ها با کارت تعریف، آمار، فیلتر، تاریخچه، فعال/غیرفعال‌سازی و همگام‌سازی محدود بازطراحی شد.
- رجیستری مرکزی فایل با جدول‌های `files`، `file_relations` و `file_audit_logs` اضافه شد؛ بارگذاری، دانلود امن، ویرایش متادیتا، ارتباط با رکورد، نسخه‌سازی، بایگانی، حذف نرم و بازیابی در پنل مدیریت فایل در دسترس است.
- آپلودهای مدیریت‌شده از مدارک هویتی، رسید پرداخت، گفت‌وگو، حقوقی، فروشگاه و فایل‌های مستقیم مدیریت در رجیستری ثبت می‌شوند؛ تصویر محصول نیز به محصول مرتبط می‌شود.

## 1.3.7

- نصب تازه و repair سازگاری installer برای ستون‌های چرخه‌عمر قرارداد تکمیل شد تا `previous_status` و `cancellation_metadata_json` در نصب‌های جدید و نیمه‌تمام نیز ایجاد شوند.
- migration ترمیمی چرخه قرارداد در updater به‌صورت idempotent اجرا و وضعیت آن پس از DDL به شکل قابل بازیابی ثبت می‌شود.
- مسیرهای health زنده و آماده‌به‌کار، صفحه‌های خطای استاتیک و قواعد پاسخ کنترل‌شده برای محیط‌های بدون دیتابیس تثبیت شدند.
- حذف دائمی قرارداد با پرداخت یا سابقه حقوقی در سرور مسدود و عملیات لغو با ثبت metadata، audit و صف outbox ایمن‌سازی شد.
- سازگاری ویرایشگر قرارداد با نسخه‌های Quill موجود تثبیت شد؛ محتوای HTML ذخیره‌شده اکنون بدون نمایش تگ خام در ویرایشگر و چاپ قرارداد بارگذاری می‌شود.
- نسخه cache سرویس‌ورکر و manifest با نسخه هسته `V1.3.7` همگام شدند.

## 1.3.6

- هسته مدیریت خطا با شناسه پیگیری، پاسخ HTML و JSON امن، پوشش خطای fatal، صفحه RTL واکنش‌گرا و fallback استاتیک 500/503 اضافه شد.
- مسیرهای ناشناخته، CSRF، دسترسی و خطای صفحه/افزونه اکنون کد HTTP صحیح و پیام کاربرپسند بدون نمایش مسیر سرور، SQL یا stack trace می‌دهند.
- محاسبات مالی حساس قرارداد، قسط، دیرکرد و رسید کارت‌به‌کارت به اعداد صحیح تومان و نرخ درصد ثابت تبدیل شد؛ جداکننده‌های فارسی نیز پشتیبانی می‌شوند.
- DDL زمان اجرا از مدل‌های مالی هسته حذف و به نصب یا migration منتقل شد تا بازدید عادی از صفحه باعث تغییر schema نشود.
- اعلان‌ها و hookهای قرارداد، قسط و رسید پرداخت پس از commit از طریق `system_outbox` پردازش می‌شوند تا شکست سرویس جانبی، تراکنش مالی را خراب نکند.
- لایه مشترک mobile/modal با scroll lock، focus management، Escape، Tab trap، bottom-sheet موبایل، safe area و baseline کنترل‌ها برای هسته و افزونه‌های نصب‌شده اضافه شد.
- تست‌های مالی، پاسخ خطا، تست استاتیک و release gate قابل اجرا به فرایند انتشار اضافه شدند.

## 1.3.5

- مسیر پرداخت دستی قسط از اعلان و هوک‌های جانبی جدا شد تا خطای پلاگین یا Notification دیگر ثبت مالی را به HTTP 500 تبدیل نکند.
- کلید ضدتکرار `payment_request_uuid` برای فرم‌های پرداخت اضافه شد تا ارسال مجدد فرم، پرداخت تکراری نسازد.
- صف عملیات جانبی (`system_outbox`) و جدول درخواست پرداخت (`payment_requests`) برای ایزوله‌سازی اعلان و hookهای افزونه اضافه شد.
- بسته نصب و بروزرسانی نسخه `V1.3.5` و تست استاتیک رگرسیون پرداخت به چرخه انتشار اضافه شد.

## 1.3.4

- رجیستری قابل توسعه درگاه‌های پرداخت به هسته اضافه شد و سازگاری کامل مسیرهای قبلی زیبال حفظ شد.
- افزونه مستقل `PromaZarinpal 1.0.0` با API رسمی v4، محیط آزمایشی و عملیاتی، پرداخت تکی و گروهی و callback مستقل از session اضافه شد.
- Merchant ID با AES-256-GCM نگه‌داری می‌شود و لاگ‌ها، خروجی‌ها و دیتابیس از PAN کامل و اسرار خام پاک نگه داشته می‌شوند.
- تأییدهای کد `100` و `101` به صورت idempotent پردازش می‌شوند و پرداخت یا اعلان تکراری ایجاد نمی‌کنند.
- انتخاب درگاه در پنل مشتری، گزارش تراکنش، خروجی CSV، retry کنترل‌شده و بررسی سلامت تنظیمات افزوده شد.
- مشکل تکرار شناسه رهگیری در تخصیص پرداخت گروهی اقساط رفع شد.
- بسته پایدار هسته، بسته بروزرسانی با manifest و ZIP مستقل افزونه زرین‌پال به فرایند انتشار اضافه شد.

## 1.3.3

- حذف لاگ‌های انتخاب‌شده یا همه لاگ‌های بخش بکاپ با تایید عددی و ثبت رویداد در audit اصلی اضافه شد.
- حذف فیزیکی یک یا چند پلاگین از پوشه `plugins/` هاست با کنترل مسیر canonical، غیرفعال‌سازی و حفظ داده‌های دیتابیس اضافه شد.
- بروزرسانی ZIP پلاگین از صفحه مدیریت با چرخه دو مرحله‌ای staging و اجرای migration پیاده‌سازی شد.
- نسخه نصب‌شده و نسخه کاندید بروزرسانی در registry جدا شدند تا rescan باعث ثبت زودهنگام نسخه نشود.
- مسیرهای حذف فایل در هسته به `plugins/` و cache موقت محدود و حذف لینک نمادین مسدود شد.

## 1.3.2

- تجربه کامل افزونه Proma Accounting با زبان بصری بومی پرما، کارت‌های ۱۶ پیکسلی، فرم‌های هم‌تراز و جدول‌های واکنش‌گرا بازطراحی شد.
- داشبورد مالی، حساب کاربران، دفترکل، فروش، کمیسیون، قوانین، تنظیمات، راه‌اندازی و راهنما در دسکتاپ، موبایل و حالت تاریک یکدست شدند.
- دارایی‌های افزونه فقط روی routeهای همان افزونه بارگذاری می‌شوند و Chart.js 4 به‌صورت محلی و بدون CDN ارائه می‌شود.
- مودال تأیید عملیات مالی، نمایش صریح اثر افزایش/کاهش، loading دکمه‌ها و جلوگیری از ارسال تکراری اضافه شد.
- نسخه افزونه حسابداری به `1.2.0` ارتقا یافت؛ قواعد مالی، مانده‌ها، کمیسیون‌ها و migrationهای موجود تغییر نکردند.

## 1.3.1

- پروفایل رسمی «قرارداد فشرده یک‌صفحه‌ای» با متن ۶px، تیتر و بند مهم ۷px، line-height برابر ۱٫۲۲ و حاشیه‌های واقعی A4 اضافه شد.
- نشانه‌گذاری امن `**بند مهم**` برای متن ساده و HTML ساختاریافته، هشدار جفت‌نبودن علامت‌ها و دکمه درج بند مهم به ویرایشگر اضافه شد.
- پیش‌نمایش واقعی A4 تعداد صفحه، مرز صفحات، ارتفاع سربرگ/متن/جدول/امضا و هشدارهای مصرف فضا را نمایش می‌دهد.
- حذف نسخه بدون استفاده و بایگانی نسخه‌های ارجاع‌شده با کنترل دسترسی، CSRF، دلیل اجباری، شمارش ارجاع و گزارش ممیزی اضافه شد.
- منوهای هر افزونه زیر یک ورودی اصلی گروه‌بندی شدند تا افزونه‌های چندصفحه‌ای سایدبار را شلوغ نکنند.

## 1.3.0

- تنظیمات قرارداد از فرم عمومی جدا و به داشبورد مستقل شماره‌گذاری، قالب، چاپ، نسخه‌ها، متغیرها، پیش‌نمایش و بازسازی منتقل شد.
- سرویس قالب مؤثر با draft، publish، superseded، restore و migration قالب قدیمی اضافه شد.
- renderer معنایی متن ساده، HTML ساختاریافته امن، محدودسازی تگ/کلاس و جلوگیری از script، event attribute و inline CSS اضافه شد.
- profile چاپ مرکزی با preset فشرده، استاندارد و خوانا و کنترل معتبر حاشیه، فونت، line-height، پاراگراف، جدول، لوگو و امضا پیاده‌سازی شد.
- حاشیه پیش‌فرض چاپ به 6mm در بالا و 10mm در طرفین کاهش یافت و padding تکراری در حالت print حذف شد.
- جدول‌های بزرگ بین صفحات ادامه پیدا می‌کنند، header جدول تکرار می‌شود و فقط ردیف‌ها از شکستن محافظت می‌شوند.
- نسخه قالب از نسخه سند جدا شد؛ انتشار قالب جدید اسناد قدیمی را stale می‌کند و صف بازسازی 25تایی اسناد نهایی را بازنویسی نمی‌کند.
- ویرایشگر محلی قالب، کپی امن با fallback، درج متغیر، source mode، شمارش متن و هشدار تغییر ذخیره‌نشده اضافه شد.

## 1.2.9

- یک سیستم چیدمان و فاصله‌گذاری مشترک برای همه صفحات مدیریت، اپراتور، وکیل، مشتری و صفحات عمومی اضافه شد.
- فاصله عمودی داشبوردها، گریدها، کارت‌ها، اعلان‌ها، جدول‌ها و وضعیت‌های خالی در دسکتاپ و موبایل یک‌دست شد.
- کارت‌های داخل ردیف‌ها و گریدهای چندستونه دیگر margin قالب را با gap شبکه جمع نمی‌کنند و بلوک‌ها به هم نمی‌چسبند.
- padding کارت‌ها و فاصله کنترل‌های هدر در breakpointهای دسکتاپ، تبلت و موبایل بازتنظیم شد.
- تست ایستای جلوگیری از بازگشت مشکلات layout و گزارش ممیزی رابط کاربری ۵۴ قالب اضافه شد.

## 1.2.8

- سیستم فرم مشترک برای هسته و پلاگین‌ها اضافه شد و ورودی‌ها، selectها، input groupها و دکمه‌های مجاور روی baseline واحد قرار گرفتند.
- سربرگ چاپ قرارداد با شماره قرارداد در راست، لوگوی مجموعه در مرکز، تاریخ و نام مشتری در چپ و عنوان مستقل بازطراحی شد.
- مسیر کامل هاست از جدول عادی پلاگین حذف و فقط در بخش بسته اطلاعات فنی مدیر نمایش داده می‌شود.
- چرخه کشف، بازاسکن، بارگذاری مجدد و نصب پلاگین حذف‌شده با وضعیت‌های canonical و registry reconciliation اصلاح شد.
- اسکرول سایدبار با viewport پویا و فضای امن پایین اصلاح شد تا آخرین گزینه در دسترس بماند.
- ابزار انتشار مستقل برای ZIP نصب کامل، بسته بروزرسانی دارای manifest و ZIP پلاگین حسابداری اضافه شد.

## 1.2.7

- ارتفاع و اسکرول سایدبار اصلاح شد تا آخرین گزینه در همه‌ی ارتفاع‌های صفحه قابل مشاهده باشد.
- baseline مشترک فرم‌ها، تراز ورودی‌ها و جایگاه علامت الزامی labelها یکدست شد.
- حذف دائمی قرارداد به آیکن سریع کنار ویرایش در بالای کارت منتقل شد.

## 1.2.6

- نسخه‌گذاری semantic اصلاح شد؛ مقدار خطی قبلی `1.0.26` اکنون به `V1.2.6` تبدیل شده است.
- نصب افزونه حسابداری با اصلاح migration حساب کاربران و نمایش خطای واقعی پایدار شد.
- آیکن مدال‌ها به آیکن Award معتبر قالب متصل شد و collapse سایدبار با وضعیت واقعی قالب همگام شد.

## 1.0.25

- اصلاح ناوبری کارت قرارداد با listener تفویض‌شده و تفکیک فرم bulk؛ کلیک روی فضای کارت، نام، شماره و آمار اکنون به جزئیات قرارداد می‌رود و کنترل‌های داخلی مستقل می‌مانند.
- اصلاح لغو قرارداد با محدودکردن اصلاحیه به پرداخت‌های همان قرارداد، پشتیبانی از پیش‌پرداخت، ثبت snapshot و Audit و جلوگیری از تغییر پرداخت قراردادهای دیگر مشتری.
- وضعیت تسویه canonical شد: قرارداد دارای تعهدات کامل پرداخت‌شده به `completed` با عنوان «تسویه‌شده» می‌رود و قرارداد لغوشده هرگز دوباره فعال نمی‌شود.
- اصلاح مسیر اصلاحیه برای بازگشت ایمن `completed` به `active`، جلوگیری از پرداخت یا قسط جدید برای قرارداد تسویه‌شده و پشتیبانی از migration پرداخت‌های پیش‌پرداخت.
- نمایش آواتارها با catalog مرکزی و `<img>` واقعی، fallback کنترل‌شده و URL نسخه‌گذاری‌شده اصلاح شد.
- نسخه service worker و cache دارایی به‌روزرسانی شد و گزارش دیباگ قرارداد و آواتار اضافه شد.

## 1.0.24

- کلیک روی کارت قرارداد مستقیماً جزئیات همان قرارداد را باز می‌کند و دکمه جزئیات تکراری حذف شد؛ ویرایش کارت با آیکن مداد در گوشه بالا-چپ انجام می‌شود.
- قرارداد پس از پرداخت کامل اقساط به‌صورت خودکار بسته و غیرفعال می‌شود و هنگام ویرایش برنامه اقساط و متن چاپی قرارداد دوباره تولید می‌شوند.
- لغو قرارداد امکان ثبت اصلاحیه اختیاری برای صفر کردن پرداخت‌های موفق مشتری را با حفظ سوابق مالی فراهم می‌کند.
- مرتب‌سازی سررسیدهای گذشته بر اساس تاریخ، مبلغ و نام مشتری و کنترل‌های هم‌راستا و چک‌باکس‌های مدرن اضافه شد.
- آواتار پیش‌فرض کاربران به‌صورت پایدار و متنوع انتخاب می‌شود تا کاربران بدون آواتار دستی همیشه تصویر یکسان نبینند.

## 1.0.23

- اصلاح ویرایش قرارداد: تغییرات مالی و زمان‌بندی، اقساط پرداخت‌نشده را همگام می‌کند و اقساط پرداخت‌شده و سفارشی حفظ می‌شوند.
- حذف الزام بی‌دلیل برای وارد کردن علت ویرایش و نمایش آواتارهای تصویری با مسیر نسخه‌گذاری‌شده در همه‌ی پنل‌ها.
- یکسان‌سازی خط مبنای ورودی‌ها، دکمه‌ها و شبکه‌ی تکرارشونده‌ی فرم‌ها برای تراز بهتر در دسکتاپ و موبایل.
- اصلاح اعتبارسنجی ZIP بروزرسانی برای بسته‌هایی که ورودی‌های ریشه‌ی `./` دارند و جلوگیری از دوباره‌چسباندن timestamp به نام استاندارد بسته.

## 1.0.22

- استانداردسازی نمایش نسخه با قالب `vMAJOR.MINOR.PATCH` و نمایش نسخه نصب‌شده در تنظیمات.
- افزودن کلیدهای فعال‌سازی مستقل برای تجارت الکترونیک و صفحه لندینگ و مسدودسازی routeهای غیرفعال.
- بهینه‌سازی داشبوردهای مدیریت، اپراتور، واحد حقوقی و مشتری با KPI و اقدام‌های سریع‌تر.
- بازسازی چیدمان تنظیمات و افزودن وضعیت قابلیت‌های فروشگاه.
- افزودن شش آواتار تصویری پیش‌فرض با رنگ‌بندی متنوع برای کاربران.

## 1.0.21

- افزودن چاپ مستقیم قرارداد از کارت و لیست قراردادها با پشتیبانی از قراردادهای فعال، تکمیل‌شده و لغوشده.
- افزودن لغو امن قرارداد بدون حذف فیزیکی، لغو اقساط فعال، ثبت علت، تاریخچه تغییر، لاگ ممیزی و اعلان مشتری.
- جلوگیری از لغو قرارداد دارای پیش‌پرداخت، پرداخت تأییدشده یا قسط پرداخت‌شده و حذف قرارداد از معوقات، جریمه، مطالبات و شاخص‌های ریسک.
- جایگزینی فهرست کامل مشتریان در فرم قرارداد با جست‌وجوی Ajax محدود، انتخاب با صفحه‌کلید و بررسی تعارض کد ملی، موبایل و ایمیل.
- اصلاح کنترل‌های گزارش‌های هوشمند، حقوقی و امتیازها برای نادیده‌گرفتن قراردادهای لغوشده.

## 1.0.20

- اصلاح redirectهای سامانه روی PHP ویندوز و جلوگیری از خطای `http://index.php` پس از ورود.
- جلوگیری از خطاهای JavaScript ساعت و منوی کناری در صفحه‌هایی که این اجزا را ندارند.
- اصلاح import بسته‌های انتقالی با حفظ کامنت‌های اجرایی MySQL و کلیدهای خارجی dump.
- بسته کامل این نسخه همراه با dump اسکیمای تمیز و قابل نصب ارائه می‌شود.

## 1.0.0

- آماده‌سازی نسخه قابل نصب Proma Pay برای XAMPP و مارکت راست‌چین.
- محدودسازی دسترسی اپراتور به سررسید گذشته و گفت‌وگو.
- افزودن modal های تماس، ارجاع حقوقی و ثبت نتیجه پیگیری در سررسید گذشته.
- افزودن فیلترها و pagination بخش اقساط.
- اعمال فونت YekanBakh در پنل، فرم‌ها، modal ها، صفحه ورود، نصب و چاپ.
- تکمیل اطلاعات توسعه‌دهنده، requirements و فایل نمونه دیتابیس.
- اصلاح نصب نیمه‌کاره، حذف حرف اضافه `پ` از صفحه نصب، و ثبت لاگ خطای نصب.
- افزودن فراموشی رمز عبور با کد پیامکی و اتصال به API پنل پیامکی IPPanel.
## V1.5.7

- جزئیات قرارداد به workspace تب‌دار و واکنش‌گرا تبدیل شد؛ ستون خالیِ ضامن دیگر فضای پرداخت و اقساط را فشرده نمی‌کند.
- هر قسط مبلغ پایه، پرداخت‌شده، مانده اصل، جریمه عادی، جریمه حقوقی واقعی/احتمالی و مبلغ قابل پرداخت امروز را مستقیم نمایش می‌دهد.
- تشخیص «ارجاع رسمی بدون تاریخ» و «نرخ حقوقی مفقود» به وضعیت مالی افزوده شد؛ این خطاها صفر مالی جعلی نیستند و پرداخت ناامن را متوقف می‌کنند.
- ویرایش و ابطال تک‌قسطی به گردش کار نسخه‌دار، قفل هم‌زمانی، CSRF، Audit و حفظ تاریخچه منتقل شد؛ حذف فیزیکی قسط غیرفعال است.
- اسکریپت تسویه از View به JavaScript مرکزیِ سازگار با CSP منتقل شد و از بودجهٔ درخواست مشترک استفاده می‌کند.
