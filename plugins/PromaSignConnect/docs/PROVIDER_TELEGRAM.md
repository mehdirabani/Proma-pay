# Telegram Bot

ربات نمی‌تواند گفت‌وگو را آغاز کند. کاربر ابتدا باید Bot را Start کند و سپس chat_id با توکن کوتاه‌عمر به حساب متصل شود. Webhook با `X-Telegram-Bot-Api-Secret-Token` و update_id ضد replay محافظت می‌شود.
