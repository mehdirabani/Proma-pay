# Registry یکپارچگی امضا

برای سند و evidence، SHA-256، نسخه الگوریتم، نسخه canonicalization، HMAC اصالت و hash قبلی رخداد ثبت می‌شود. `PROMA_SIGNATURE_HMAC_KEY` حداقل ۳۲ کاراکتر دارد و فقط در محیط اجرا نگهداری می‌شود؛ این کلید در دیتابیس، export یا بسته افزونه قرار نمی‌گیرد.
