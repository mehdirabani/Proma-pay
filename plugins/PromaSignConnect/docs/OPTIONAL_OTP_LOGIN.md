# ورود اختیاری OTP

رمز فعلی مشتری، شامل رفتار سازگار چهار رقم آخر موبایل، همواره در Core باقی می‌ماند. فعال‌کردن OTP فقط یک مسیر موازی می‌سازد. تغییر session پس از موفقیت باید با primitive احراز هویت Core انجام شود.
