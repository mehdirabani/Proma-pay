# معماری امضا

Mode A پذیرش داخلی با نسخه منجمد سند، OTP، نام تایپی، هویت حساب، timestamp و زنجیره hash است. Mode B فقط interface آینده برای امضای مبتنی بر گواهی مجاز است و این نسخه آن را پیاده‌سازی یا ادعا نمی‌کند.
