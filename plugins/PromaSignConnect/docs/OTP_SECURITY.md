# امنیت OTP

کد با `random_int` ساخته و فقط با `password_hash` ذخیره می‌شود. کد در log یا payload پایدار نگهداری نمی‌شود. چالش purpose-bound، زمان‌دار، یک‌بارمصرف و دارای سقف تلاش، cooldown و محدودیت ساعتی است. fallback باید همان کد منطقی را ارسال کند.
