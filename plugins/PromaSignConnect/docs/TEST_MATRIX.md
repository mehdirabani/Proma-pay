# ماتریس تست

واحد: OTP، cipher، canonical evidence و adapter mapping. یکپارچه: MariaDB transaction، concurrent verify، multi-party، queue و lifecycle. امنیت: replay، webhook، secret leakage و دسترسی سند. دستی: اتصال واقعی هر provider فقط با credential مالک سامانه.
