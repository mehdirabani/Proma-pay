# Button alignment

Buttons remain scoped under `.proma-accounting`; final financial actions use the primary variant and an explicit operation label. Core visual regression across all pages is **NOT EXECUTED**.
