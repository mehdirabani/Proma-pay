# Database migration audit

The local Salary migration contains `user_salary_rules` and `user_salary_periods`; the latest referral/withdrawal migration is versioned and idempotent. Real MySQL/MariaDB execution, partial migration repair and SQLSTATE 2014 reproduction are **NOT EXECUTED**.
