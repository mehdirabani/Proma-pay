# Modal alignment

Accounting financial actions use the shared `pa_financial_dialog()` helper. The final action now disables itself and shows a loading label before the single confirmed POST. Full browser focus-trap and dark-mode verification is **NOT EXECUTED**.
