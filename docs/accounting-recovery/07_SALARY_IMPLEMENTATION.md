# Salary implementation

Version 1.2.13 keeps integer-Toman SalaryService posting and idempotent period keys, and redesigns the administration page with a validated user selector instead of a raw numeric ID field. Worker scheduling and missing-salary repair remain outside page load and require production worker verification.
