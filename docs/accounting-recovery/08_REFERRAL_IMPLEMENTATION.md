# Referral implementation

Administrator and customer routes are separate. Referral code generation is idempotent; customer withdrawal and bank routes are session-scoped. Notification delivery, production payout ledger policy and full contract-modal integration remain **NOT EXECUTED**.
