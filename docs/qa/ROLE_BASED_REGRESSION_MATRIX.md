# Role-based regression matrix

- Admin: can approve/reverse legal costs and publish contract templates.
- Lawyer: can open permitted internal cases, add file/cost to an accessible
  case, but cannot approve their own cost.
- Customer: cannot open internal legal documents, costs or template settings.
- All roles: a direct URL must receive the same server authorization result as
  the menu path.
