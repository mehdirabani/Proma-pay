# Contract editor test report — v1.5.10

Static release checks cover: visual default, A4 preview surface, variable
browser/search, protected variable serialization path, server HTML sanitizer,
and component-scoped contract tabs. Manual browser acceptance must cover a
long RTL template, visual → HTML → visual round trip, two guarantors in
preview/print, safe paste and 768px/mobile layouts before production release.
