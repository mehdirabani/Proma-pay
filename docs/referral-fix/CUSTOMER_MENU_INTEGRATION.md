# Customer menu

The customer navigation now contains one server-rendered `portal/referrals` item labelled «کسب درآمد». It uses the existing app layout and creates no AJAX menu request. The plugin route remains separate from the administrator route.
