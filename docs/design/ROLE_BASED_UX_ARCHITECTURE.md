# Role-based UX architecture

Administrative financial controls and legal operations share server-side
services but should not share unrestricted actions. The UI may streamline the
workflow per role; authorization is never inferred from the visible menu.
Internal legal drafts are explicitly distinct from official external filing.
