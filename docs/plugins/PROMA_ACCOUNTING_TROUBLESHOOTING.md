# رفع خطای Proma Accounting

## کمیسیون ساخته نمی‌شود

فروشنده فعال، قانون، زمان شناسایی و مبلغ مبنا را بررسی کنید. برای زمان پس از قسط یا تسویه باید رویداد پرداخت تکمیل شده باشد.

## کمیسیون وارد مانده نمی‌شود

اگر نیاز به تأیید فعال است، ابتدا کمیسیون را تأیید کنید. سپس ثبت خودکار یا دکمه «ثبت در حساب» را بررسی کنید.

## migration اجرا نمی‌شود

نسخه PHP، دسترسی دیتابیس، InnoDB و جدول `system_plugin_migrations` را بررسی کنید. migration `2026_07_13_accounting_v1_1.sql` idempotent است و داده تاریخی را حذف نمی‌کند.

## اختلاف مبلغ

breakdown preview را بررسی کنید: مبلغ مبنا، خام، اثر حداقل، اثر حداکثر و اثر گردکردن جدا نمایش داده می‌شوند.
