# نصب افزونه زرین‌پال

1. ابتدا Proma Pay را به `V1.3.4` یا جدیدتر بروزرسانی کنید.
2. `PromaZarinpal-1.0.0.zip` را در «پلاگین‌ها» بارگذاری و بررسی کنید.
3. دکمه نصب را بزنید تا migrationهای افزونه اجرا شوند.
4. افزونه را فعال کنید؛ فعال‌سازی درگاه بانکی هنوز خاموش می‌ماند.
5. تنظیمات Merchant و Callback را ثبت و سپس درگاه را فعال کنید.

نصب جدول‌های `proma_zarinpal_settings`, `proma_zarinpal_transactions` و `proma_zarinpal_logs` را ایجاد می‌کند. حذف عادی افزونه این تاریخچه را نگه می‌دارد.
