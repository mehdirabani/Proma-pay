# ممیزی منبع حقیقت — Proma Pay

تاریخ ممیزی: ۱۴۰۵/۰۵/۱۰ (۲۰۲۶-۰۸-۰۱)

## مبنای انتخاب‌شده

| مورد | مقدار مشاهده‌شده |
| --- | --- |
| شاخهٔ کاری | `codex/release-v1.4.5` |
| commit پایه | `ed9ef29205c59f2b31812139967ffd99605a7d86` |
| پیام commit پایه | `release: harden Proma Pay v1.4.5` |
| نسخهٔ فعلی سورس | `1.5.7` در `config/version.php` |
| نسخهٔ asset | `1.5.7` در `config/settings.php` |
| نسخهٔ دیتابیس اعلام‌شده | `2026.08.01` |
| API افزونه | `1.0` |
| Manifest افزونه حسابداری | `1.2.14` در `plugins/PromaAccounting/plugin.json` |

## وضعیت قابل اثبات

- درخت کاری پیش از این ممیزی دارای تغییرات بسیار زیادِ commit‌نشده بود؛ این ممیزی هیچ‌کدام از تغییرات نامرتبط را بازنویسی یا حذف نمی‌کند.
- بسته‌های موجود در `dist/core` تا V1.5.5 هستند؛ نسخهٔ سورس V1.5.7 به معنی تأیید یا انتشار آن نیست.
- فایل ورودی `PromaPay-v1.5.3(1).zip` در ریشه وجود دارد، اما منبع توسعهٔ انتخاب‌شده سورس محلی فعلی است، نه محتویات یک ZIP بدون نصب و تطبیق دیتابیس.
- نسخه و وضعیت رجیستریِ نصب‌شدهٔ افزونه، migrationهای واقعاً اجراشده، schema زنده و نسخهٔ Production از این workspace قابل مشاهده نیست.

## تفاوت‌ها و ریسک‌ها

1. نام شاخه و commit پایه از نسخهٔ سورس عقب‌تر هستند؛ از این رو نباید از نام شاخه برای تعیین نسخهٔ قابل انتشار استفاده شود.
2. بستهٔ کاملِ V1.5.2 حدود ۸۵ مگابایت است و بسته‌های بعدی کوچک‌ترند. اختلاف حجم، به‌تنهایی نشانهٔ ناقص بودن بسته نیست؛ محتویات و نصب واقعی باید معیار باشد.
3. تا اتصال به MariaDB آزمایشی و یک سرور HTTP محلی برقرار نشود، هیچ ادعایی دربارهٔ migration، نصب نهایی، به‌روزرسانی، rollback یا browser E2E قابل تأیید نیست.
4. این تغییرات تا گذر از دروازهٔ انتشار، تنها اصلاحات سورس هستند؛ خروجی Stable جدید تولید نخواهد شد.

## مسیر تصمیم

1. اجرای تست‌های ایستا و unit محلی.
2. اجرای integration با `PROMA_TEST_DB_DSN` در MariaDB مجزا.
3. اجرای browser/visual با سرور QA محلی.
4. تطبیق نصب تازه و update از ZIP نهایی.
5. فقط پس از شواهد کامل، افزایش نسخه و ساخت artifact.
