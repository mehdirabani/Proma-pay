# Smart Notification System

Notifications are recipient-owned records with delivered, seen, read, archived, deleted and actioned timestamps. A recipient/dedupe key prevents duplicate reminders.

Opening a notification marks the recipient copy read before target navigation. Archive and delete affect only the current recipient. Badges count visible unread copies and zero badges are omitted.
