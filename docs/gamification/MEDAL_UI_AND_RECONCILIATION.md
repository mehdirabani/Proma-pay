# Medal UI and Reconciliation

Medal definitions declare permanent or reversible behavior plus revocation and reactivation policies. The no-overdue medal is reversible, automatically revoked when its condition fails and restored when eligibility returns.

Award, revoke and restore operations use one award row and append immutable history. Reconciliation can target one customer or a bounded active-customer batch. The admin UI uses aligned cards, structured modal sections and accessible color controls.
