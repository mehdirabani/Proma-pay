# تطبیق تراکنش زرین‌پال

مدیر می‌تواند تراکنش‌های `verification_failed`, `callback_received`, `verifying` یا `request_uncertain` دارای Authority را حداکثر پنج بار Verify کند. تأیید عددی لازم است. این عملیات درخواست یا Authority تازه نمی‌سازد و تاریخچه log و audit را حفظ می‌کند.
