# جریان پرداخت چندقسطی زرین‌پال

هسته فقط یک `payment_group` و افزونه فقط یک درخواست زرین‌پال ایجاد می‌کنند. پس از Verify، `PaymentGroupService` مبلغ را ابتدا به اقساط انتخابی و سپس اقساط بعدی همان قرارداد تخصیص می‌دهد. مبلغ بیشتر از کل بدهی قبل از انتقال به درگاه رد می‌شود.
