# جریان Callback زرین‌پال

Callback عمومی فقط `Authority` و `Status` را می‌پذیرد. `NOK` تراکنش را لغوشده نگه می‌دارد. `OK` وضعیت را `callback_received` کرده و Verify سرور به سرور را آغاز می‌کند. نتیجه `100` نهایی و نتیجه `101` فقط در صورت تطابق تراکنش ذخیره‌شده بازیابی می‌شود.
