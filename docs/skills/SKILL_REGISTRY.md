# Project skill registry

Discovered on 2026-09-22 in `skill/`. User delivery order: v2.0.0 redesign and banners first; v2.0.1 modernization second.

| Actual skill | Role / contribution | Scope | Required verification |
| --- | --- | --- | --- |
| adaptive-skill-orchestrator 1.0.0 | Foundation / 5% | Route work by risk; manager-specialists workflow | Explicit scope and conflict resolution |
| project-graph-memory 1.1.0 | Foundation / 5% | Incremental context and dependency leads | Freshness; heuristic relationships are not proof |
| html-css-tailwind-pro 2.0.0 | Primary / 45% | RTL components, responsive redesign; Tailwind only in v2.0.1 | Build, computed layout, keyboard and browser checks |
| php-engineering-skill 4.0.0 | Specialist / 30% | Banner persistence, uploads, updater and cache | PHP lint, real MariaDB, authorization and rollback |
| hardline-security-qa-tester 2.0.0 | Validator / 15% | Changed trust boundaries and release artifacts | Negative tests and evidence-qualified release verdict |

The security validator retains veto authority regardless of contribution weight. PHP money remains integer Toman; no parallel frontend calculation engine. User-requested customer login behavior is preserved.

## Conflict resolution

- User's latest delivery order supersedes the earlier single v2.0.1 release plan. Bootstrap remains in v2.0.0; its removal is NOT claimed for that package.
- Skill discovery means reading every actual SKILL.md and routing targeted references, not loading every unrelated example/eval into context.
- The graph is a local development artifact, excluded from Git and release ZIPs. It cannot justify exporting configuration or user data.
- Deliberately invalid skill test fixtures are not production PHP and are excluded from application lint/package inventory.
- Speed does not authorize changing payment rules, removing authorization, or labelling unexecuted tests as passed.

Supporting installed skills: Playwright for headless browser verification, security-best-practices for browser JavaScript. No Figma URL or hosted design file was supplied; the supplied images are visual references, not Figma resources.
