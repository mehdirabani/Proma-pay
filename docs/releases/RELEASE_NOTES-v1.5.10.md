# Proma Pay v1.5.10

- Made the contract editor visual-first, replacing the raw-HTML default shown
  in the reported production screen.
- Added protected visual variable tokens, variable search/browser and local
  sandboxed A4 layout preview.
- Kept the server renderer, sanitizer, version history, draft/publish and
  print path authoritative and backward compatible.
- Scoped and hardened contract tabs to avoid global panel collisions; added
  accessible ARIA wiring, keyboard navigation and guarded URL-hash state.
- Added static release checks and audit/QA documentation for the editor and
  legal role workflow.
